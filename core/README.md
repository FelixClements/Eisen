# Core Library

Platform-agnostic reference implementation: mutation/merge model, HLC, canonical parsers, device identity, epoch-root/keys, nonce reservation, signed encrypted envelopes, manifest-chain verification, encrypted local store, and mutation transactions.

Owner: project owner / platform
