# Core Library

Cross-platform Rust core: reference mutation/merge model, HLC, canonical parsers, device identity, epoch-root/keys, nonce reservation, signed encrypted envelopes, manifest-chain verification, encrypted local store, and mutation transactions.

Exposed through a small C API for Android (JNI) and Windows (P/Invoke).

Owner: project owner / platform
